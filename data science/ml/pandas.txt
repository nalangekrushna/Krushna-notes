Pandas

df.groupby('column name')  = same as group by in sql
df.merge  =  joins in database
loc = location ( index value)
iloc = index location ( row number )
iat[0,1] = setting value by position.
